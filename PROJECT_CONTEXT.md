# Fibre Ready — บริบทโปรเจกต์และการทำงานต่อ

อัปเดตบริบท: 8 กันยายน 2026 เอกสารนี้อธิบายแอปที่สร้างแล้ว ไม่ใช่รายการสิ่งที่ต้องเริ่มสร้างใหม่

## ถ้าผู้ใช้ถามว่าโฟลเดอร์นี้เกี่ยวกับอะไร

ตอบได้ว่า:

> โฟลเดอร์นี้เป็นโปรเจกต์ Fibre Ready เว็บภาษาไทยสำหรับเตรียมตัวก่อนเริ่มงาน Call Center / Support ของ AIS Fibre ภายใน 5 วัน เปิด `index.html` ใช้ออฟไลน์ได้เลย มีบทเรียน คำศัพท์แบบ Flashcard ข้อสอบ 100 ข้อ ระบบสุ่มเน้นจุดอ่อน และกิจกรรมภาพโต้ตอบ เช่น อ่านไฟ ONT ทดลอง WiFi และซ้อมเลือกขั้นตอนรับสาย บันทึกความคืบหน้าในเบราว์เซอร์และเลือกธีมมืดได้ในหน้าตั้งค่า เชื่อมกับ GitHub repo `sahachartksm/test` บนสาขา `main`

เนื้อหาเป็นแผนทบทวนส่วนตัว ไม่ใช่คู่มืออบรมหรือข้อสอบทางการของ AIS

## ตำแหน่งและไฟล์

- โฟลเดอร์จริง: `C:\Users\test1\Desktop\job_apply\ais`
- พาธที่ผู้ใช้เขียนบ่อย: `C:\Users\test1\Desktop\job\_apply\ais` ให้เข้าใจตามหมายเหตุใน `AGENTS.md` และตรวจ filesystem ก่อน
- `index.html`: แอปทั้งหมด รวม CSS, JavaScript, เนื้อหา ข้อสอบ และ inline SVG ไม่มี dependency ภายนอก
- `AGENTS.md`: จุดเริ่มอ่านของ AI และข้อกำหนดการทำงาน
- `PROJECT_CONTEXT.md`: เอกสารฉบับนี้
- `.git`: repository ของโปรเจกต์นี้เอง ไม่เกี่ยวกับ repository niyaime ที่อาจเป็น cwd เริ่มต้นของเครื่องมือ

การเปิดใช้งาน: ดับเบิลคลิก `index.html` หรือเปิด `file:///C:/Users/test1/Desktop/job_apply/ais/index.html` ในเบราว์เซอร์ ไม่ต้องรัน server

## สิ่งที่ผู้ใช้ต้องการและมีแล้ว

### เส้นทางเรียน 5 วัน

| วัน | เนื้อหา | กิจกรรมภาพที่เพิ่มแล้ว |
| --- | --- | --- |
| 1 | IP, DNS, DHCP, LAN/WAN, Router, Switch, Modem และ ONT | ตามรอยข้อมูล 4 ขั้น ตั้งแต่รับค่าเครือข่าย ค้นชื่อ ส่งคำขอ จนรับคำตอบ พร้อมลองเลือกคำตอบก่อนอ่านเหตุผล |
| 2 | ADSL เทียบ Fiber/FTTx, ONT/ONU, splitter และตารางไฟ | แผงไฟ ONT/Router จำลอง 5 ชุด มีสีและข้อความค้าง/กระพริบ/ดับ ให้เลือกจุดที่ควรเริ่มตรวจ |
| 3 | 2.4GHz/5GHz, SSID, ปัญหา WiFi, Restart และ Factory Reset | เปลี่ยนระยะและผนัง เลือกย่าน แล้วดูภาพ ตำแหน่งมือถือ แถบสัญญาณและเหตุผล |
| 4 | Flow ตรวจไฟ → สาย → Restart → ทดสอบ → ส่งต่อ, ศัพท์ IT | เคสรับสาย 3 แบบ: Restart แล้วหาย, LOS ต่อเนื่อง, ไฟเลี้ยงปิด แต่ละเคสมีเส้นทางแก้และผลลัพธ์ต่างกัน |
| 5 | Cheat sheet รวมวันที่ 1–4 และข้อสอบรวบยอด | จับคู่อาการ 4 แบบกับ DHCP, DNS, ทางแสง/LOS หรือ WiFi พร้อมอธิบายจุดเริ่มตรวจ |

แต่ละวันมีแท็บ **เนื้อหา / คำศัพท์ / แบบทดสอบ** อยู่ในหน้าเดียวกัน วันที่ 5 รวมคำศัพท์ที่ไม่ซ้ำจากวันก่อนหน้า

วันที่ 4 มีลิงก์เปิดแท็บใหม่ไป 10fastfingers เป้าฝึก 20 คำต่อนาที เป้านี้เป็นแผนส่วนตัว ไม่ใช่เกณฑ์ทางการของบริษัท ลิงก์ภายนอกและแหล่งอ้างอิงต้องใช้เน็ต แต่การเรียนในแอปไม่ต้องใช้

### ระบบข้อสอบ

- `QUESTION_BANK` มี 100 ข้อ วันที่ 1–4 วันละ 25 ข้อ แต่ละข้อมี `id`, `day`, `question`, `options` 4 ตัวเลือก, `answer` เป็น index 0–3 และ `explanation`
- ID รูปแบบ `d1-01` ถึง `d4-25` ใช้อ้างอิงสถิติ ห้ามเปลี่ยน ID เพียงเพราะปรับถ้อยคำ
- ไม่มีข้อสอบคำนวณ เน้นความเข้าใจ ความหมายอุปกรณ์/ไฟ และสถานการณ์ลูกค้า
- **ฝึกตามวัน:** เลือกวันที่ 1–4 และจำนวน 10 / 20 / ทั้งหมด 25 ข้อ
- **รวบยอด:** 15 / 20 / 40 / ทั้งหมด 100 ข้อ ปุ่มลัดจากวันที่ 5 เริ่ม 20 ข้อ ทุกชุดรับประกันว่ามีคำถามจากวันที่ 1–4 ครบ
- **เน้นจุดอ่อน:** เฉพาะกลุ่มยังไม่ชำนาญจากทุกวัน รวมข้อที่ยังไม่เคยทำ เลือก 10 / 20 / ทั้งหมด ถ้ามีน้อยกว่าจำนวนที่เลือก ใช้เท่าที่มี ถ้าไม่มีแล้วปุ่มเริ่มจะ disabled พร้อมข้อความอธิบาย
- สุ่มโดยไม่ซ้ำในชุด สลับลำดับข้อและลำดับตัวเลือกใหม่เมื่อเริ่มชุด ตัวเลือกที่แสดงใช้ index เฉลยเดิมในการตรวจคำตอบ
- ตอบแล้วล็อกข้อนั้น แสดงถูก/ผิดพร้อมเหตุผลทันที บันทึกคำตอบและสถิติในการเขียน localStorage ครั้งเดียว
- พักไปอ่านบทเรียนหรือปิดเบราว์เซอร์แล้วกลับมาทำชุดเดิมได้ การ reload ข้อที่ตอบแล้วไม่เพิ่มสถิติซ้ำ
- ต้องทำจบชุดจึงบันทึกคะแนนรวมและตัดสินผ่าน หน้าผลมีจำนวนถูก/ผิด เปอร์เซ็นต์ รายการข้อผิดพร้อมเฉลย และแถบภาพรวมการจำ

### เกณฑ์ผ่านและ progress

- เกณฑ์ผ่านใช้คะแนนจริง **อย่างน้อย 80%** ไม่ใช้เปอร์เซ็นต์ที่ปัดแล้วมาพิจารณา
- โหมดตามวันบันทึกคะแนนสูงสุดและสถานะผ่านของวันที่เลือก (1–4)
- โหมดรวบยอดที่มีอย่างน้อย 15 ข้อและครบทุกหัวข้อใช้ผ่านวันที่ 5
- โหมดจุดอ่อนอัปเดตสถิติการจำ แต่ไม่ให้คะแนนผ่านรายวัน
- คะแนนสูงสุดไม่ลด แม้ฝึกซ้ำแล้วได้คะแนนต่ำลง
- Progress ของวัน = อ่านจบ 25% + พลิกความหมายคำศัพท์ตามสัดส่วน 25% + คะแนนสูงสุดเทียบเกณฑ์ 80% อีกสูงสุด 50%
- Progress รวมเป็นค่าเฉลี่ยของทั้ง 5 วัน สถานะ “ผ่าน” กับ “อ่าน/คำศัพท์ครบ” เป็นคนละเรื่อง จึงอาจสอบผ่านแต่ progress ยังไม่เต็ม

### การทบทวนตามประวัติคำตอบ

สถิติแต่ละข้อมี `correct`, `wrong`, `streak`, `lastCorrect` และตัดกลุ่มตามลำดับต่อไปนี้:

1. **ยังไม่ชำนาญ:** ยังไม่เคยทำ หรือตอบล่าสุดผิด หรือยอดผิดสะสมมากกว่าถูก
2. **จำได้แม่นแล้ว:** ไม่เข้าเงื่อนไขข้อแรก และตอบถูกติดกันตั้งแต่ 3 ครั้งขึ้นไป
3. **กำลังจำได้:** ไม่เข้าเงื่อนไขข้างต้น โดยปกติเป็นการตอบถูกติดกัน 1–2 ครั้ง

น้ำหนักสุ่มรายข้อ: ยังไม่ชำนาญ 6 / กำลังจำได้ 3 / แม่นแล้ว 1 ข้อที่แม่นแล้วยังมีโอกาสออก โหมดรวบยอดเลือกหนึ่งข้อจากแต่ละวันก่อนเติมส่วนที่เหลือแบบถ่วงน้ำหนักแล้วสลับลำดับทั้งหมด ระบบนี้ไม่ได้มีปฏิทินนัดทบทวนหรือการแจ้งเตือนตามเวลา

### กิจกรรมภาพและ animation

- ใช้ HTML, CSS, inline SVG และ JavaScript เท่านั้น ไม่มีภาพหรือ library ที่ต้องโหลดจากเว็บ
- ผู้เรียนลองเลือกก่อนอ่านคำอธิบาย ตอบผิดแล้วเลือกใหม่ได้ ตอบถูกแล้วจึงไปขั้นต่อไป
- WiFi เป็นแบบจำลองเชิงคุณภาพ ไม่ใช่การคำนวณกำลังคลื่น ระยะจริง หรือ Mbps และไม่ได้จำลองสภาพรบกวนทั้งหมด
- สีไฟมีข้อความกำกับเสมอ ตารางและแบบฝึกเตือนให้ยึดคู่มือรุ่นจริง
- Animation เกิดจากการกดและเล่นสั้น ๆ เช่น packet เคลื่อนหรือไฟกระพริบ มีปุ่มเล่นไฟซ้ำ และปิดการเคลื่อนไหวตาม `prefers-reduced-motion`
- `state.activityDone` เก็บสถานะทำกิจกรรมจบของวันนั้นแยกต่างหาก ไม่แก้คะแนนหรือ progress สูตรเดิม
- ตัวแปร `labs` เก็บขั้นที่กำลังลองในหน่วยความจำของหน้าปัจจุบัน สลับหน้าแล้วกลับมายังอยู่ แต่ reload จะเริ่มกิจกรรมรอบใหม่ โดยสถานะทำจบยังอยู่
- วัน 2 ทำจบเมื่ออ่านถูกครบ 5 ชุดไฟในรอบ วัน 3 เมื่อทดลองครบทั้งสองย่านในอย่างน้อยสองสภาพระยะ/ผนัง วัน 4 เมื่อจบเคสใดเคสหนึ่ง

## ธีมและการจัดเก็บ

ผู้ใช้เลือก **ตั้งค่า → ธีมการแสดงผล → สว่าง / มืด** ค่าเริ่มต้นสว่าง ไม่ได้ผูกอัตโนมัติกับธีมระบบ

ใช้ localStorage คีย์เดียว `fibre-ready:v1` โครงสร้างหลัก:

```text
version: 1
theme: "light" | "dark"
activityDone: { [day]: true }
days: { [day]: { read, seen[], flashIndex, best, passed, attempts } }
stats: { [questionId]: { correct, wrong, streak, lastCorrect } }
active: ชุดคำถาม ลำดับตัวเลือก คำตอบที่เลือก index ปัจจุบัน และสถานะจบ | null
history: คะแนนชุดที่ทำจบล่าสุดไม่เกิน 20 ชุด
view: หน้าล่าสุด
```

- โหลดข้อมูล v1 เก่าที่ไม่มี `theme` หรือ `activityDone` ได้ โดยเติมค่าเริ่มต้นและเก็บความคืบหน้าเดิม
- สคริปต์เล็กใน `<head>` อ่านธีมก่อนวาดหน้า เพื่อลดการกระพริบของพื้นสว่าง
- `loadState()` ตรวจและทำความสะอาดข้อมูลที่โหลดมา `save()` แจ้งเตือนถ้าพื้นที่เก็บข้อมูลใช้ไม่ได้ แอปยังทำงานในหน่วยความจำได้
- ข้อมูลเป็นของไฟล์และเบราว์เซอร์นั้น ไม่ซิงก์ข้ามเครื่อง การย้ายไฟล์ ล้างข้อมูล หรือใช้โหมดไม่ระบุตัวตนอาจทำให้ข้อมูลเดิมไม่อยู่
- ปุ่มรีเซ็ตอยู่ใน Settings มี dialog ยืนยัน ลบเฉพาะคีย์ของแอปและเริ่ม runtime ของกิจกรรมใหม่ ไม่ใช้ `localStorage.clear()` กับข้อมูลผู้ใช้

## แผนที่โค้ดสำหรับแก้ต่อ

เลขบรรทัดเปลี่ยนได้ ให้ค้นชื่อด้วย `rg` แทนยึดเลขบรรทัดเก่า:

| ส่วน | ชื่อที่ควรค้น |
| --- | --- |
| รูปแบบและธีม | `:root`, `html[data-theme="dark"]`, `.activity`, `prefers-reduced-motion` |
| เนื้อหาและคำศัพท์ | `DAYS`, `lesson`, `lessonExtra`, `vocab` |
| คลังข้อสอบ | `QUESTION_BANK`, `byId` |
| ข้อมูลผู้เรียน | `STORAGE_KEY`, `freshState`, `loadState`, `save`, `validSession` |
| ธีม | `applyTheme`, `appearanceSettings`, `theme-select` |
| Progress และการจำ | `dayProgress`, `mastery`, `memoryCounts`, `memoryPanel` |
| สุ่มและทำข้อสอบ | `shuffle`, `weightedSample`, `createSession`, `answerQuestion`, `finishSession`, `creditDay`, `results` |
| ข้อมูลกิจกรรม | `NETWORK_STEPS`, `LED_CASES`, `CASE_STEPS`, `MATCH_CARDS` |
| กิจกรรมและสถานะ | `newLabs`, `learningActivity`, `handleLabAction`, `handleLabChange`, `refreshActivity`, `activityComplete` |
| การแสดงหน้า | `dashboard`, `dayPage`, `settings`, `render`, `go` และ event delegation ท้ายไฟล์ |

เส้นทางใช้ URL hash: `#dashboard`, `#day/1/content`, `#day/1/vocab`, `#day/1/quiz`, `#run`, `#results`, `#settings` ไม่ใช้ framework หรือ routing package

## การตรวจงานที่ใช้แล้ว

เคยทดสอบกับ Chrome แบบ headless โดยเปิด `file://` และบล็อกเครือข่าย พร้อมตรวจ UI ที่ความกว้าง 320, 390, 768 และ 1440 พิกเซล ทั้งสองธีม

ผลตรวจล่าสุดของแอปที่ commit `929ba3e`:

- ข้อสอบครบ 100 ข้อ วันละ 25 ข้อ ตัวเลือกครบ 4 ข้อและ ID ไม่ซ้ำ
- ทำข้อสอบจริง: เฉลยทันที, เกณฑ์ 80%, เก็บคะแนนสูงสุด, สรุปข้อผิด, ทั้งสามโหมด, การสลับข้อ/ตัวเลือก และน้ำหนักสุ่ม
- Reload และปิดเปิดเบราว์เซอร์ใหม่แล้วทำต่อได้โดยไม่เพิ่มสถิติซ้ำ
- Flashcard, การบันทึกอ่านจบ, รีเซ็ตเฉพาะแอป, ข้อมูลเสีย และกรณี storage ถูกบล็อก
- เปลี่ยนธีมแล้วเก็บข้อมูลการเรียนเดิมได้
- กิจกรรมครบ 5 วัน เคสไฟครบ 5 ชุด เคสรับสายครบ 3 เส้นทาง บันทึกสถานะจบโดยไม่แก้คะแนนสอบ
- ไม่พบหน้าเว็บล้นจอแนวนอน ไม่พบ JavaScript runtime error และไม่มีคำขอโหลดทรัพยากรผ่าน HTTP จากตัวแอป
- ตรวจ reduced motion แล้ว animation ของ packet, คลื่น และไฟถูกปิด

เครื่องมือทดสอบอยู่ **นอก repository** ที่โฟลเดอร์ชั่วคราวในเครื่องเดิม:

```powershell
node C:\Users\test1\AppData\Local\Temp\fibre-ready-audit\test.cjs
node C:\Users\test1\AppData\Local\Temp\fibre-ready-audit\theme-test.cjs
node C:\Users\test1\AppData\Local\Temp\fibre-ready-audit\activity-test.cjs
```

ไฟล์เหล่านี้ใช้ Playwright และ Chrome ที่ติดตั้งในเครื่อง พร้อมเก็บภาพหน้าจอในโฟลเดอร์ชั่วคราวเดียวกัน ไม่ใช่ dependency ของเว็บ และอาจหายได้เมื่อระบบล้าง temp ตรวจว่ามีอยู่ก่อนใช้งาน อย่ารับรองว่าผ่านการทดสอบใหม่โดยอ้างเฉพาะผลเก่า

หากเครื่องมือเดิมไม่อยู่ ให้ใช้เบราว์เซอร์ที่มีตรวจตามรายการส่วนที่เปลี่ยน และตรวจ syntax ของ **ทุก `<script>`** (มีทั้งสคริปต์ธีมใน head และสคริปต์แอปท้าย body) การ format ผ่านอย่างเดียวไม่ยืนยันว่า JavaScript ใช้งานได้

การเปลี่ยนเอกสารอย่างเดียวไม่ต้องรัน UI tests ซ้ำ ส่วนการแก้คะแนน/storage ต้องตรวจการทำข้อสอบและกลับมาเปิดใหม่ด้วย

## Git และการส่งมอบ

- Remote `origin`: `https://github.com/sahachartksm/test.git`
- สาขาทำงาน: `main`
- ผู้ใช้เคยอนุญาตให้แทนที่ประวัติเดิมของ `main` เพื่อเริ่มโปรเจกต์นี้ใหม่ และได้ทำไปแล้ว ไม่ใช่งานที่ต้องทำซ้ำทุกครั้ง
- `c044cd4`: root commit ของเว็บออฟไลน์พร้อมธีมสว่าง/มืด ได้ push เข้า `main` แล้วในเซสชันสร้างแอป
- `929ba3e`: เพิ่มกิจกรรมภาพและสถานการณ์รับสาย ได้ commit ในเครื่องแล้ว ในการตรวจสถานะก่อนเขียนเอกสารนี้ local `main` อยู่ ahead ของ `origin/main` 1 commit
- เอกสารบริบทถูกเพิ่มภายหลัง commit ข้างต้น สถานะปัจจุบันอาจเปลี่ยนแล้ว ให้ตรวจ Git ก่อนตอบผู้ใช้หรือส่งงาน
- แยกให้ชัดว่า “เขียนไฟล์”, “commit ในเครื่อง” และ “push ขึ้น GitHub” เป็นคนละสถานะ อย่าบอกว่างานอยู่บน GitHub หากยังไม่ได้ push
- ไม่มีงานแก้ฟีเจอร์ที่ทราบว่ายังค้างจากคำขอก่อนหน้า งานใหม่ให้เริ่มจากคำขอใหม่ของผู้ใช้

คำสั่งตรวจสถานะโดยใช้ working directory ที่ถูกต้อง:

```powershell
git -C C:\Users\test1\Desktop\job_apply\ais status --short --branch
git -C C:\Users\test1\Desktop\job_apply\ais log -5 --oneline
git -C C:\Users\test1\Desktop\job_apply\ais remote -v
```

## รูปแบบการทำงานที่ผู้ใช้ต้องการ

ผู้ใช้สื่อสารเป็นภาษาไทย ต้องการให้ลงมือแก้และตรวจให้เรียบร้อยโดยไม่ต้องเล่าสเปคเดิมทุกครั้ง ให้คงเว็บที่ใช้งานง่ายบนมือถือ เน้นความเข้าใจจริงและ active learning ไม่เพิ่ม dependency เพียงเพื่อทำภาพประกอบ สรุปสิ่งที่เปลี่ยน ผลตรวจ และสถานะ Git แบบสั้นและตรงกับความจริง
