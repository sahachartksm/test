# บริบทสำหรับ AI ที่ทำงานในโฟลเดอร์นี้

โปรเจกต์นี้คือ **Fibre Ready** เว็บทบทวนภาษาไทยก่อนเริ่มงาน Call Center / Support ของ AIS Fibre ในเวลา 5 วัน เป็นเว็บออฟไลน์แบบ Vanilla HTML/CSS/JavaScript ไฟล์เดียว: `index.html`

## เริ่มเซสชันใหม่

1. อ่าน [PROJECT_CONTEXT.md](PROJECT_CONTEXT.md) ก่อนอธิบายโปรเจกต์หรือแก้โค้ด เอกสารนี้รวมความต้องการที่ตกลงกัน โครงสร้าง ระบบคะแนน กิจกรรม และวิธีตรวจงาน
2. ตรวจ `git status --short --branch` และ `git log -5 --oneline` เพื่อทราบงานค้างและสถานะจริง อย่าใช้สถานะ Git ในเอกสารแทนการตรวจล่าสุด
3. หากผู้ใช้ส่งเพียงพาธแล้วถามว่า “เกี่ยวกับอะไร” ให้ตอบสรุปเป็นภาษาไทยจากบริบทนี้ได้เลย ไม่ต้องให้ผู้ใช้เล่าความต้องการเดิมซ้ำ และไม่ต้องแก้ไฟล์เพียงเพราะถามข้อมูล

## พาธที่ต้องเข้าใจให้ตรงกัน

- พาธจริงในเครื่องที่สร้างโปรเจกต์: `C:\Users\test1\Desktop\job_apply\ais`
- ผู้ใช้อาจพิมพ์ `C:\Users\test1\Desktop\job\_apply\ais` ซึ่งในบทสนทนานี้หมายถึงโฟลเดอร์เดียวกัน แต่พาธที่มีโฟลเดอร์ `job\_apply` แยกจริง ๆ ไม่มีอยู่ ณ เวลาจัดทำเอกสาร ให้ตรวจพาธจริงก่อนดำเนินการ
- เป็นคนละโปรเจกต์กับ `D:\Project\niyaime` แม้ IDE จะเปิดไฟล์ devlog ของ niyaime อยู่ อย่าแก้หรือ commit โปรเจกต์นั้นแทน
- Git remote: `https://github.com/sahachartksm/test.git` สาขาที่ใช้งานคือ `main`

## ข้อกำหนดที่ต้องรักษา

- ตัวแอปต้องจบใน `index.html` เปิดจาก `file://` ได้ทันที ไม่ต้อง build, server, CDN, npm หรืออินเทอร์เน็ต เอกสารประกอบแยกไฟล์ได้
- รักษาธีมสว่าง/มืด ความอ่านง่ายบนมือถือ และข้อความภาษาไทย คำเทคนิคใช้ภาษาอังกฤษได้
- รักษาข้อมูลเดิมใน localStorage คีย์ `fibre-ready:v1` และ ID ข้อสอบเดิมเมื่อแก้เนื้อหา
- ห้ามเพิ่มโจทย์คำนวณ เช่น subnetting หรือคำนวณ IP คำถามควรเน้นความเข้าใจและสถานการณ์รับสาย
- อย่าทำให้ Restart/Reboot กับ Factory Reset กลายเป็นอย่างเดียวกัน และอย่าเหมารวมความหมายไฟทุกยี่ห้อ/รุ่น
- ภาพสอนใช้ inline SVG/CSS/JavaScript ได้ กิจกรรมต้องมีการให้ผู้เรียนลองทำและเห็นคำอธิบาย ไม่พึ่งสีหรือ animation อย่างเดียว รองรับ `prefers-reduced-motion`
- กิจกรรมการเรียนรู้แยกจากคะแนนสอบและเกณฑ์ผ่านเดิม
- แก้เฉพาะงานที่เกี่ยวข้อง รักษางานค้างของผู้ใช้ ทดสอบตามส่วนที่เปลี่ยน และรายงาน commit กับ push ให้ตรงกับสิ่งที่ทำจริง
- เมื่อเปลี่ยนพฤติกรรมสำคัญ ให้อัปเดต `PROJECT_CONTEXT.md` โดยสรุปสภาพสุดท้าย ไม่ต้องเก็บบทสนทนาทุกข้อความ
